name: cinder-delete-db-job
version: 0.4.0-1
description: Helm chart to delete the cinder database
keywords:
  - openstack
  - cinder
  - cinder-delete-db
sources:
  - http://github.com/openstack
engine: gotpl
#icon: A URL to an SVG or PNG image to be used as an icon (optional). make this point to the new project icons when ready
