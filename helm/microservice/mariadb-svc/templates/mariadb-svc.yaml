apiVersion: v1
kind: Service
spec:
  ports:
    - port: {{ .Values.mariadb_port }}
      name: {{ .Values.element_name | quote }}
  selector:
    service: {{ .Values.element_name | quote }}
metadata:
   name:  {{ .Values.element_name | quote }}
