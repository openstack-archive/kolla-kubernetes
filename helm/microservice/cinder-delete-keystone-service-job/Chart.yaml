name: cinder-delete-keystone-service-job
version: 0.7.0-1
description: Helm chart to delete the cinder keystone service
keywords:
  - openstack
  - cinder
  - delete
  - service
sources:
  - http://github.com/openstack
engine: gotpl
#icon: A URL to an SVG or PNG image to be used as an icon (optional). make this point to the new project icons when ready
