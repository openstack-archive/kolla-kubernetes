name: horizon-backend-svc
version: 0.5.0-1 #FIXME make this changable
description: Horizon backend service for kolla-kubernetes
keywords:
  - openstack
  - horizon
  - backend
  - svc
sources:
  - http://github.com/openstack
engine: gotpl
#icon: A URL to an SVG or PNG image to be used as an icon (optional). make this point to the new project icons when ready
