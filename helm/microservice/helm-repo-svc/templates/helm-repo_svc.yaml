{{- $searchPath := ":global.kolla.helm-repo.svc:global.kolla.helm-repo.all:global.kolla.helm-repo.all:global.kolla.all" }}
{{- $elementPort := .Values.helm_repo_port | default 8879 }}
{{- $elementService := "helm-repo" }}
{{- $elementName := $elementService }}
{{- $elementType := .Values.type }}
{{- with $env := dict "element_port" $elementPort "element_name" $elementName "element_service" $elementService "element_service_type" $elementType "element_port_external" .Values.element_port_external "kolla_kubernetes_external_vip" .Values.kolla_kubernetes_external_vip "Values" .Values "Release" .Release "searchPath" $searchPath }}
{{- include "common_svc" $env }}
{{- end }}
