{{- $serviceName := "keystone" }}
{{- $serviceComponent := "public" }}
{{- $serviceKind := "svc" }}
{{- $elementName := $serviceName }}
{{- with $env := dict "serviceName" $serviceName "serviceComponent" $serviceComponent "serviceKind" $serviceKind "elementName" $elementName "Values" .Values "Release" .Release }}
{{- include "common_svc" $env }}
{{- end }}
