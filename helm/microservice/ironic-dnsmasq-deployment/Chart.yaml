name: ironic-dnsmasq-deployment
version: 0.5.0-1
description: ironic dnsmask deployment microservice package
keywords:
  - openstack
  - ironic
  - dnsmask
sources:
  - http://github.com/openstack
engine: gotpl
#icon: A URL to an SVG or PNG image to be used as an icon (optional). make this point to the new project icons when ready
