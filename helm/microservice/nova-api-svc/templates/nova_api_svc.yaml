{{- $serviceName := "nova" }}
{{- $serviceType := "api" }}
{{- $serviceKind := "svc" }}
{{- $elementName := $serviceName }}
{{- with $env := dict "serviceName" $serviceName "serviceType" $serviceType "serviceKind" $serviceKind "elementName" $elementName "Values" .Values "Release" .Release }}
{{- include "common_svc" $env }}
{{- end }}
