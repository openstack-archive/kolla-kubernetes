{{- $searchPath := ":global.kolla.horizon.backend.deployment:global.kolla.horizon.backend.all:global.kolla.horizon.all:global.kolla.all" }}
{{- $imagePullPolicy := include "kolla_val_get_str" (dict "key" "image_pull_policy" "searchPath" $searchPath "Values" .Values ) }}
{{- $containerConfigDirectory := include "kolla_val_get_str" (dict "key" "container_config_directory" "searchPath" $searchPath "Values" .Values ) }}
{{- $selectorKey := include "kolla_val_get_str" (dict "key" "selector_key" "searchPath" $searchPath "Values" .Values ) }}
{{- $selectorValue := include "kolla_val_get_str" (dict "key" "selector_value" "searchPath" $searchPath "Values" .Values ) }}
{{- $replicas := include "kolla_val_get_str" (dict "key" "replicas" "searchPath" $searchPath "Values" .Values ) }}
{{- $instanceVersion := include "kolla_val_get_str" (dict "key" "instance_version" "searchPath" $searchPath "Values" .Values ) }}
{{- $c := dict "searchPath" $searchPath "Values" .Values }}
{{- $_ := set $c "contName" "horizon" }}
{{- $_ := set $c "imageName" "image_full" }}
{{- $_ := set $c "tagName" "image_tag" }}
{{- $imageFull := include "kolla_build_image_full" $c }}
{{- $resourceName := "horizon-backend" }}
{{- $netHostTrue := false }}
{{- $podTypeBootstrap := false }}
{{- $elementName := .Values.element_name | default $resourceName }}
{{- with $env := dict "netHostTrue" $netHostTrue "podTypeBootstrap" $podTypeBootstrap "resourceName" $resourceName "Values" .Values "Release" .Release "searchPath" $searchPath }}
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ $elementName }}
spec:
  replicas: {{ $replicas }}
  selector:
    matchLabels:
      service: {{ $elementName }}
  template:
    metadata:
      labels:
        service: {{ $elementName | quote }}
        type: {{ $instanceVersion | quote }}
      annotations:
        pod.beta.kubernetes.io/init-containers: '[
{{- include "common_dependency_container_single" $env | indent 10 }}
        ]'
    spec:
      nodeSelector:
        {{ $selectorKey }}: {{ $selectorValue | quote }}
      containers:
      - name: main
        image: "{{ $imageFull }}"
        imagePullPolicy: {{ $imagePullPolicy | quote }}
        volumeMounts:
{{- include "common_volume_mounts" $env | indent 10 }}
          - mountPath: {{ $containerConfigDirectory }}
            name: horizon-config
        env:
{{- include "common_env_vars" $env | indent 10 }}
{{- include "common_containers" $env | indent 6 }}
      volumes:
{{- include "common_volumes" $env | indent 8 }}
        - name: horizon-config
          configMap:
            name: horizon-backend-{{ $instanceVersion }}
            items:
            - key: horizon.conf
              path: horizon.conf
            - key: config.json
              path: config.json
            - key: local-settings
              path: local_settings
{{- end }}

