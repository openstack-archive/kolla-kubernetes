name: cinder-volume-lvm-daemonset
version: 0.7.0-1
description: cinder volume lvm microservice layer
keywords:
  - openstack
  - cinder
  - cinder-volume-lvm
sources:
  - http://github.com/openstack
engine: gotpl
#icon: A URL to an SVG or PNG image to be used as an icon (optional). make this point to the new project icons when ready
