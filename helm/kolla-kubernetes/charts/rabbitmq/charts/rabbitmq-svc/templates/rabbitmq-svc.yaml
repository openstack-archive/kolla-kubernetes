apiVersion: v1
kind: Service
spec:
  ports:
    - port: {{ .Values.global.rabbitmq_port }}
      name: rabbitmq
  selector:
    service: rabbitmq
metadata:
   name: rabbitmq
   namespace: {{ .Values.global.kolla_kubernetes_namespace }}
