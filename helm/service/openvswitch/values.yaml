global:
    kolla:
        all:
            kubernetes_entrypoint: true
            type: network
        openvswitch:
            vswitchd:
                all:
                    dependencies:
                        socket:
                        - /run/openvswitch/db.sock
