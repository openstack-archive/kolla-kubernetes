apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.element_name }}
spec:
  type: {{ .Values.type }}
  ports:
  - port: {{ .Values.port }}
    protocol: TCP
    name: {{ .Values.element_name }}
  selector:
    service: {{ .Values.element_name }}
    type: api
