global:
    kolla:
        all:
            kubernetes_entrypoint: true
            element_name: memcached
        memcached:
            deployment:
                dependencies:
                    service:
                    - memcached

