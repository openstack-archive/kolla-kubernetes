global:
    kolla:
        all:
            kubernetes_entrypoint: true
        memcached:
            deployment:
                kubernetes_entrypoint: true
                dependencies:
                    service:
                    - memcached

