global:
    kolla:
        all:
            kubernetes_entrypoint: true
            global.kolla.all.pv_enabled: true
        mariadb:
            statefulset:
                    dependencies:
                        jobs:
                        - mariadb-init-element
