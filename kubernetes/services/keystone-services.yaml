apiVersion: v1
kind: Service
spec:
  ports:
    - port: 35357
  selector:
    name: keystone-admin
metadata:
   name: keystone-admin

---
apiVersion: v1
kind: Service
spec:
  ports:
    - port: 5000
  selector:
    name: keystone-public
metadata:
   name: keystone-public