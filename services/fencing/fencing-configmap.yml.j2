apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ kolla_kubernetes.cli.args.resource_name }}
  namespace: {{ kolla_kubernetes_namespace }}
data:
  fencing.conf: |
    config 1
  config.json: |
    config 2
