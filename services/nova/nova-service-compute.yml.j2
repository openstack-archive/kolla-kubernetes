apiVersion: v1
kind: Service
spec:
  externalIPs:
    - {{ kolla_kubernetes_external_vip }}
  ports:
    - port: {{ nova_api_port }}
      name: nova-api
  selector:
    service: nova
    type: api
metadata:
   name: nova-api
