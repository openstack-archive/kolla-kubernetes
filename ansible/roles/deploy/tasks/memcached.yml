---
- name: Create memcached Service
  command: "helm install --debug ../helm/microservice/memcached-svc --namespace kolla --name memcached-svc --values /etc/kolla/cloud.yaml"

- name: Create memcached Physical Volume Claim
  command: "helm install --debug ../helm/microservice/memcached-deployment --namespace kolla --name memcached --values /etc/kolla/cloud.yaml"
