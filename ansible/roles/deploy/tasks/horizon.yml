---
- name: Start horizon service
  command: "helm install --debug ../helm/microservice/horizon-svc --namespace kolla --name horizon-svc --values /etc/kolla/cloud.yaml"

- name: Start horizon deployment
  command: "helm install --debug ../helm/microservice/horizon-deployment --namespace kolla --name horizon-deployment --values /etc/kolla/cloud.yaml"
