---
- name: Bootstrapping MariaDB
  command: kolla-kubernetes bootstrap mariadb

- name: Bootstrapping MariaDB
  command: kolla-kubernetes resource status pod mariadb
  register: result
#  until: result.results.status == "Succeeded"
#  retries: 10
#  delay: 4

- debug: var=result
