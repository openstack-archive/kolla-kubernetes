---
- name: Generate configuration files
  hosts: localhost
  connection: local
  roles:
    - rabbitmq
    - nova
    - neutron
    - keystone
    - glance
    - horizon
    - memcached
    - cinder
    - heat
